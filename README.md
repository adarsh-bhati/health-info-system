# Health Assistant

Minimal Flask app with:
- Bootstrap frontend
- Flask auth and admin
- RAG (FAISS + sentence-transformers)
- PDF ingestion
- Groq AI client (stub)
- SQLite via SQLAlchemy

Run:

```bash
python main.py
```

Configure `config.py` with your `GROQ_API_KEY`.